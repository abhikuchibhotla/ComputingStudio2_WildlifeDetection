# Core ML / vision
torch>=2.3.0
torchvision>=0.18.0
ultralytics>=8.4.0          # YOLO
timm>=1.0.0                 # vision model backbones

# Depth estimation (install manually from source)
# git clone https://github.com/apple/ml-depth-pro
# pip install -e ./ml-depth-pro/src    # provides depth_pro

# Image processing
Pillow>=10.0.0
opencv-python>=4.9.0        # cv2

# Web app
streamlit>=1.44.0

# Utilities
numpy>=1.26.0
psutil>=5.9.0
requests>=2.32.0

# Alpamayo (local/private package — install separately)
# pip install -e /models/Alpamayo-1.5-10B   or equivalent
